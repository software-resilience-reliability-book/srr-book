# What Belongs in Version Control?

---

Note that artifacts that can be rebuilt (e.g. `bin` and `obj` directories) should not be tracked in version control.

Before gitignore:

![explorer pane before gitignore](../images/source-control-mess.png)
